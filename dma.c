#include <stdio.h>
#include <stdlib.h>

// Q. Program to allocate memory to store 5 prices..
/*
int main() {
    float *ptr;
    ptr = (float *) malloc(5 * sizeof(float));

    ptr[0] = 1;
    ptr[1] = 12;
    ptr[2] = 9;
    ptr[3] = 8;

    for(int i=0; i<4; i++)
        printf("%f\n", ptr[i]);
}
*/

//Q. Program to allocate memory of size n , where n = user input
/*
int main() {

    float *ptr;
    float n;
    printf("ENTER n : ");
    scanf("%f", &n);

    
    ptr = (float *) malloc(5 * sizeof(float));


    for(int i=0; i<n; i++)
        printf("%f\n", ptr[i]);
}
*/


